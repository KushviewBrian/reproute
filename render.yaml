services:
  - type: web
    name: reproute-api
    runtime: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: .
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: ENVIRONMENT
        value: production
      - key: POC_MODE
        value: "true"
      - key: CORS_ALLOW_ORIGINS
        value: https://reproute.onrender.com
      - key: GEOCODE_WORKER_URL
        value: https://photon.komoot.io/api/
      - key: ORS_BASE_URL
        value: https://api.openrouteservice.org
      - key: ORS_API_KEY
        sync: false
